setup:  addons:    - plan: caddy:2.4.2-alpine      as: caddybuild:
  docker:
    web: Dockerfile.flask    caddy: docker/caddy/Dockerfile.alpine
# run:  # web: python /app/run.py
run:  caddy: ash /start_caddy.sh  web: python /app/run.py