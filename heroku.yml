setup:  addons:    - plan: caddy:2.4.2-alpine      as: caddybuild:
  docker:
    web: Dockerfile.flask
# run:  # web: python /app/run.py
run:  web: python /app/run.py  worker:    image: web